<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:checkableBehavior="single">

        <item android:id="@+id/toolbar_login"
            android:icon="@drawable/ic_cloud_login"
            android:title="@string/access_cloud"
            app:showAsAction="never"/>
        <item android:id="@+id/toolbar_help"
            android:icon="@drawable/ic_help"
            android:title="@string/help_menu" />
        <item android:id="@+id/toolbar_editor_mode"
            android:icon="@drawable/ic_build"
            android:title="@string/editing_mode"
            android:checkable="true"/>
        <item android:id="@+id/toolbar_restart"
            android:icon="@drawable/ic_refresh"
            android:title="@string/restart_menu"/>
        <item android:id="@+id/toolbar_dictionary"
            android:icon="@drawable/ic_dvr"
            android:title="@string/dictionary_menu"/>
        <item android:id="@+id/toolbar_background_image"
            android:icon="@drawable/ic_image"
            android:title="@string/background_image"/>
    </group>

</menu>